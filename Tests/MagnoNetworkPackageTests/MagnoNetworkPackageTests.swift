import XCTest
@testable import MagnoNetworkPackage

final class MagnoNetworkPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MagnoNetworkPackage().text, "Hello, World!")
    }
}
